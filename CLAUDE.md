# 전국 학과 지도 — 개발 기록

## ① 앱의 목적과 대상

**전국 대학의 학과를 갈래로 묶어 보는 진학 상담 도구.** 수업용 앱이 아니다.

- 주 사용자: 담임·진학 담당 **교사**. 옆에 앉은 **학생·학부모**가 함께 본다.
- `2028_entrance` 에서 갈라져 나온 앱이지만 **자료가 완전히 다르다** — 아래 저작권 절 참고.
- 성취기준을 겨냥한 앱이 아니라 **진학 상담 업무 도구**다.

### 이 앱이 푸는 문제

학생은 「컴퓨터공학과」 하나만 알고 온다. 그런데 전국에는 같은 갈래인데
**이름이 다른 학과가 205종** 있다 — `AI・컴퓨터공학과` · `정보컴퓨터공학부` ·
`글로벌AI컴퓨터공학과` · `소프트웨어학부 컴퓨터공학전공` …
**이름만 보고는 같은 갈래인지 알 수 없어서 학생이 갈 수 있는 길을 놓친다.**

---

## ★★ 저작권 — 이 앱의 가장 중요한 성질

**이 앱은 공개 배포한다.** `2028_entrance` 와 정반대다.

| | `2028_entrance` | `major-map` (이 앱) |
|---|---|---|
| 자료 | 여러 선생님·기관의 입시 자료 | 교육부 대학알리미 **공공데이터** |
| 조건 | 「공교육기관에서만」·「사교육 활용 비동의」 | **공공저작물 제1유형(출처표시)** |
| 배포 | **비공개**, `dist/index.html` 파일로만 전달 | **GitHub Pages 공개** |

⚠⚠ **입시 자료를 섞지 말 것.** 입결·전형계획·권장과목을 한 줄이라도 넣는 순간
공개 배포를 할 수 없게 된다. 그래서 이 앱에는 그런 자료가 **하나도 없다.**
「합격 가능성」을 말하지 않는 것도 같은 까닭이다.

⚠ **출처 표시를 빼지 말 것.** 공공저작물 제1유형의 **유일한 조건**이다.
빌드 뒤 `vite.config.js` 의 `copyright-guard` 가 `dist/index.html` 안에
「대학알리미」가 있는지 검사한다. 표시하는 곳은 세 군데 —
화면 푸터 · 「이 앱에 대하여」 화면 · 스크립트 `/*!` 배너.

---

## ② 기능 목록 (탭 5 · 화면 6)

| # | 탭 | 화면 | 해시 |
|---|---|---|---|
| 1 | 🗺️ 계열 지도 | 대 6 → 중 28 → 소 160 파고들기 | `#jido/gyeyeol` |
| 2 | 🔎 학과 찾기 | **핵심 화면** — 같은 갈래인데 이름이 다른 학과 | `#chatgi/hakgwa` |
| 3 | 🔤 이름 비교 | 갈린 이름을 한 줄로 모으기 | `#ireum/bigyo` |
| 4 | 🏫 대학별 보기 | 대학 하나의 학과 전부 + 계열 분포 | `#daehak/byul` |
| 5 | 📖 안내 | 이 앱에 대하여 / 표준분류 전체 | `#annae/jaryo` `#annae/bunryu` |

### ★ 「유사 학과」를 잇는 두 가지 방법 — 섞지 않는다

**어느 근거인지 모르면 학생이 잘못 믿는다.** 그래서 따로 보여 준다.

1. **표준분류계열** (`소분류줄`) — 원자료에 이미 있는 값. **우리가 지어낸 것이 아니다.**
2. **이름 열쇠** (`열쇠줄`) — 괄호·꼬리를 떼고 어간 비교. **어림이다.**

둘 다 「같다」고 한 것은 `이웃학과().둘다` 로 뽑아 **🎯 거의 확실히 같은 학과**로 맨 위에 둔다.

⚠ **분류가 없는 학과(`분류 없음`)는 분류 기준으로 묶지 않는다.**
묶으면 자유전공과 광역모집이 통째로 한 학과가 된다. 시험이 이것을 지킨다.

---

## ③ 현재 개발 상태와 다음 할 일

**2026-09-01 — 1차 완성.** 탭 5 · 화면 6 · 시험 59가지 · 소스 10개 ·
빌드 결과 `dist/index.html` **703KB** 한 파일(외부 자원 0개).

### 다음으로 미룬 것
- **커리어넷 학과정보 연결** — 학과 소개글·관련직업·관련자격증을 붙이면
  「이 학과가 무엇을 배우나」까지 답할 수 있다. OpenAPI 인증키가 필요하다.
- **학과 신설·폐과 이력** — 대학알리미 「학교별 학부·학과 리스트」에 2014년부터 있다.
- **지도(그래프) 시각화** — 지금은 표와 알약이다. 힘겨운 그래프(force-directed)로
  그리면 눈에는 좋으나 이름이 겹쳐 읽기 어려워질 수 있다. 먼저 표로 쓸모를 확인한 뒤에.

---

## ④ 자료 — `tools/make_data.py`

```bash
python tools/make_data.py   # source/키워드별 학과정보.xlsx → src/data/major.json (772KB)
```

원본은 [공공데이터포털 15119002](https://www.data.go.kr/data/15119002/fileData.do)
「교육부_대학알리미_키워드별 학과정보」. XLSX, 무료, 로그인 불필요.

| | 값 |
|---|---|
| 원본 | 29,475행 (대학원 포함) |
| **학부만** | **16,221행** · 대학·캠퍼스 **408곳** · 학과명 **8,161종** |
| 표준분류계열 | 대 **6** → 중 **28** → 소 **160** |
| 분류 없음(N.C.E.) | 1,918행 (**11.8%**) |
| `major.json` | 772KB |

### 만들며 겪은 것

- ⚠ **대학원을 빼야 한다.** 원본 29,475행 중 13,254행이 일반·특수·전문대학원이다.
  안 빼면 「학과 수」가 통째로 틀어진다. 시험이 이 숫자를 지킨다.
- ⚠ **N.C.E. 를 지우지 말 것.** 「분류 없음」으로 남긴다. 자율전공·광역모집이
  대부분이라 2028 무전공 확대에서 **오히려 중요한 덩어리**다.
- ⚠ **학과명을 중복 없이 담는다.** 16,221번 나오는 이름이 8,161종뿐이라,
  번호로 바꾸니 960KB → 772KB 로 줄었다.
- ⚠ **이름 정규화는 자바스크립트 쪽 한 곳에서만** 한다(`src/lib/data.js` 의 `이름열쇠()`).
  파이썬에도 같은 규칙을 두면 **반드시 어긋난다.** 미리 만들어 담으면 자료도 150KB 커진다.

---

## ⑤ 계산 — `src/lib/data.js`

### ⚠⚠ 이름 열쇠 — 꼬리는 **덩어리로만** 뗀다

```js
const 꼬리들 = ['학과', '학부', '전공', '계열', '학군', '과정', '대학', '학교', '과']
```

**「학」·「과」 한 글자를 떼면 안 된다.** 「컴퓨터공학」이 「컴퓨터공」이 되고,
무엇보다 **떼는 자리에 따라 뭉치가 달라진다.**
「간호학부(간호학전공)」처럼 두 겹으로 붙은 이름이 있어 **두 번까지** 뗀다.

시험이 지키는 값 —
`컴퓨터공학과 = 컴퓨터공학부`, `간호학과 = 간호전공 = 간호학과(야간) = 간호학부(간호학전공)`,
`국어교육과 ≠ 국어국문학과`, `기계공학과 ≠ 기계과`.

### ★ 원자료 안에서 분류가 어긋난 곳

**같은 뜻인데 분류가 다르다.**
「컴퓨터공학과」(59곳)는 `전산학・컴퓨터공학`, **「컴퓨터공학부」(19곳)는 `응용소프트웨어공학`** 이다.
**몰래 고치지 않는다.** 원자료 그대로 보여 주고, 이럴 때를 위해
**이름 기준 묶음을 함께** 두었다. 두 방법을 나란히 두는 까닭이 바로 이것이다.

### 성능

`모든줄`(16,221개)은 **모듈을 처음 부를 때 한 번만** 편다.
`이름뭉치()` 는 5,947 열쇠를 훑으므로 탭 ③ 에서 **한 번 만들어 두고 다시 쓴다**
(`뭉치들` 변수). 화면을 그릴 때마다 부르면 눈에 띄게 느리다.

---

## 만들 때 지킨 규칙

`2028_entrance/docs/탭-만들기.md` 와 같다. `src/lib/ui.js`·`widgets.js`·`style.css` 는
그 앱에서 가져와 머리 주석만 바꿨다.

- ⚠ **`window.addEventListener('resize')` · `setInterval` 을 탭 파일에서 직접 쓰지 말 것.**
  `ui.js` 의 `onResize()` · `screenInterval()` 을 쓴다.
- ⚠ **`alert`·`confirm`·`prompt` 금지.** 크롬이 주소를 함께 보여 준다.
- ⚠ **`localStorage` 금지.** 이 앱은 아무것도 저장하지 않는다.
- ⚠ **`h()` 자식 자리에 HTML 태그 문자열을 넣지 말 것.** 글자 그대로 나온다.
- ⚠ **시험은 탭 파일(DOM)을 부르지 않는다.** 탭 파일의 문법 오류는 빌드에서야 터진다.
  **빌드 전에 반드시 `npm run check:syntax`.**

⚠ **탭 파일이 서로를 부른다** — `t1`·`t3`·`t4` 가 `t2-chatgi.js` 의 `학과열기()` 를 쓴다.
`t2` 는 `data.js` 만 부르므로 고리(circular)가 생기지 않는다. **이 방향을 뒤집지 말 것.**

---

## 명령

| 명령 | 하는 일 |
|---|---|
| `npm install` | 의존성 설치 |
| `npm run dev` / `npm start` | 개발 서버 |
| `npm run build` | `dist/index.html` 한 파일로 빌드 |
| `npm test` | 시험 59가지 |
| `npm run check:syntax` | 소스 전수 문법·규칙 검사 |
| `npm run data` | `source/` 원자료 → `src/data/major.json` (python + openpyxl) |

빌드가 끝나면 `copyright-guard` 가 **저작권 표시**와 **원자료 출처(대학알리미)**를 검사한다.
저작권 표시는 낱말로 **14회**(소스마다 `/*!` 머리 주석이 남는다), 출처 「대학알리미」는 **7회**다.
⚠ `grep -c` 는 **줄 수**를 세므로 4가 나온다. 낱말 수를 보려면 `grep -o 티쳐무 dist/index.html | wc -l`.
⚠ vite 콘솔이 찍는 「539.97 kB」는 **바이트가 아니라 글자 수**다. 한글이 많아 실제는 703KB.
⚠ `esbuild.legalComments` 를 `'none'` 으로 두면 `/*!` 배너까지 지워진다.

### 저장소
https://github.com/trmoo/major-map — **공개(public)**.
Pages: https://trmoo.github.io/major-map/
⚠ **빈 저장소를 만들 때 Settings → Pages → Source 를 「GitHub Actions」 로 먼저 켜 둘 것.**
브랜치 배포로 두면 빌드 안 한 원본이 서비스되어 **흰 화면**이 된다
(`saints_dict`·`api-practice` 에서 실제로 겪었다).
